version: '2'
services:
  # ruby:
  #   build: .
  #   volumes:
  #     - /var/www/blog

  nginx:
    image: nginx
    # depends_on:
    #   - 'ruby'
    environment:
      - NGINX_HOST=blog.ojiry.com
      - NGINX_PORT=80
    ports:
      - '8080:80'
    volumes:
      - /var/www/blog/current/_site:/usr/share/nginx/html
