version: '2'
services:
  ruby:
    build: ./docker/ruby
    volumes:
      - /var/www/blog

  nginx:
    build: ./docker/nginx
    depends_on:
      - 'ruby'
    ports:
      - '8080:80'
    volumes:
      - /var/www/blog

